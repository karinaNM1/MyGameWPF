<Page x:Class="MyGame.PageAdmin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyGame"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageAdmin">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="3*"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

       

        <WrapPanel Orientation="Vertical" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <Button x:Name="btnQuestion" Style="{StaticResource btn}" Content="Вопрос" Margin="10, 5" MinWidth="100" Click="btnQuestion_Click"/>
            <Button x:Name="btnCategory" Style="{StaticResource btn}" Content="Категории" Margin="10, 5" MinWidth="100" Click="btnCategory_Click"/>
            <Button x:Name="btnComplexity" Style="{StaticResource btn}" Content="Сложность" Margin="10, 5" MinWidth="100" Click="btnComplexity_Click"/>
        </WrapPanel>

    </Grid>
</Page>

